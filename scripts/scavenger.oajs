function scavenger()
{


var list = Orion.FindList('sca', 'ground','',3)
    if (!list.length)
        return false;

    for (var i = 0; i < list.length; i++)
    {
 
        Orion.MoveItem(list[i], 0, backpack);
        //Orion.Wait(1000);
    }

    return true;
}